import PropTypes from 'prop-types';

function ProjectCard({ isWhiteTheme }) {
  return (
    <div>ProjectCard</div>
  )
}

ProjectCard.propTypes = {
    isWhiteTheme: PropTypes.bool.isRequired
}

export default ProjectCard;